spring:
  liquibase:
    change-log: classpath:db/changelog-master.xml
    enabled: true
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:postgresql://localhost:5432/social_network
    username: postgres
    password: P@ssw0rd