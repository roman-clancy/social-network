spring:
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:postgresql://localhost:5432/social_network?createIfNotExists=true
    username: postgres
    password: P@ssw0rd